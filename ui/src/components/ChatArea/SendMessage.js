import React from 'react'

const SendMessage = () => {
  return (
    <div className='send-message'>
        <input type="textarea" />
        <button>Send</button>
    </div>
  )
}

export default SendMessage;