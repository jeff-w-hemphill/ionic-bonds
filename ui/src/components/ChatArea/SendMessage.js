import React from 'react'

const SendMessage = () => {
  return (
    <div className='send-message'>
        <input type="text" />
        <button>Send</button>
    </div>
  )
}

export default SendMessage;